# 운영 절차서 — 행사 당일 대비

## 1. OSRM 캐시 워밍 (행사 24시간 전 권장)

### 배경
무료 OSRM 공용 엔드포인트(`router.project-osrm.org`)는 ~600 req/min 제한.
행사 당일 09:00 동시에 50명+ 라이브 지도 진입 시 일부 fetch 실패 가능.

각 클라이언트는 OSRM 응답을 localStorage 에 자동 캐시(`osrm:<dir>:<path>`).
사전에 한 명이 캐시를 생성하고 다른 운영자/사용자가 같은 path 로 fetch 하면
공유는 안 되지만, 자체 캐시는 영구 보관(LS 용량 한계 내).

### 절차
**행사 전날까지** 운영자가 각 단말에서 1회씩 수행:

1. 승객 페이지 (또는 운영자 모드의 시연 모드) 진입
2. `?view=map` 으로 라이브 지도 열기
3. **양방향 노선 폴리라인 그려질 때까지 대기** (5~10초)
   - 도로 곡선이 정류장 직선보다 진해지면 OSRM 응답 완료
4. 페이지 새로고침 → 즉시 그려지면 캐시 OK
5. 운영자 본인 단말 + 백업 단말 모두 수행

### 검증
LS 키 확인 (브라우저 개발자 도구 → Application → Local Storage):
```
osrm:outbound:126.91...;126.94...;126.95...   → 800-byte JSON
osrm:inbound:126.95...;126.91...               → 800-byte JSON
```

캐시 키는 정류장 + via 좌표 순서로 결정. **경유점 변경 시 새 키로 재요청**
(이전 캐시 무효 — 행사 전 viapoints 확정 필수).

---

## 2. 행사 전 점검 체크리스트 (D-1)

### Firebase
- [ ] `database.rules.json` 최신 내용 콘솔 게시
- [ ] `/drivers/{UID}` · `/admins/{UID}` 화이트리스트 등록 확인
- [ ] 운영자 단말 로그인 테스트 → `authTokenRef` 정상 발급 확인
- [ ] 사용 통계 백업 (이전 회차 데이터 분리 보관)

### 운영자 단말
- [ ] 운영자 모드 진입 (`?admin=1` 또는 제목 7-탭)
- [ ] **시연 모드 (`FAKE_GPS`) 해제 확인** — 자료 탭 「시연·테스트 모드」 → 「시연 모드 해제」
- [ ] `BUILD` 화면 우하단 표시가 최신 빌드인지 확인
- [ ] **위치 공유 ON + 자동 모드 ON** 상태에서 셔틀 운행 정상 동작 시험
- [ ] 시간표 모드 자동 회차 전환 시험 (오프라인 백업)
- [ ] 안내 음성 톤 / 음량 / mp3 클립 정상 재생 확인
- [ ] OSRM 캐시 워밍 (위 1번 절차)

### 승객 화면
- [ ] 메인 페이지 D-X 배너 표시 확인
- [ ] 시간표 모달 양방향 7회차 (일요일 5회차) 표시
- [ ] 행사장 페이지 8개 구역 + 프로그램 카드 정상
- [ ] 라이브 지도 진입 → 양방향 노선 + 정류장 마커 + 구역 폴리곤
- [ ] 운영자 위치 송신 시 승객 지도에서 🚌 마커 노출
- [ ] 푸시 알림 (스티커 등록 시 운영자에게 토스트 + chime)

### 백업
- [ ] 운영자 다단(driver / admin) 별도 단말 1대씩
- [ ] 4G/5G + Wi-Fi 듀얼 백업
- [ ] 로컬 멘트 mp3 클립 사전 다운로드 (오프라인 대비)

---

## 3. 행사 당일 운영

### 06:00 ~ 09:00 (사전)
- 운영자 단말 켜고 영구 페이지 진입
- 자료 탭 → 시각 기준 회차 재설정 (현재 시각으로 첫 회차 정렬 확인)
- 자동 모드 + 위치 공유 ON

### 09:00 ~ 18:00 (운영)
- **자동 모드**: GPS 자동 안내 + 자동 회차 전환 + LIVE 송신
- 자료 탭 「📋 정류장별 안내 타이밍」 으로 진입/도착/통과 타이밍 조정 가능
- 스티커 알림 토스트 → StickerEditor 열어 정리

### 18:00 이후 (마감)
- 운행 종료 송신 (운영 모드 → 위치 공유 OFF)
- 사용 통계 추출 (자료 탭 → 「사용자 페이지 사용 통계」)

---

## 4. 행사 종료 후 (6.15 이후)

- StickerEditor 「만료 정리」 1회 실행 → 모든 스티커 자동 사라짐 (행사 종료 ts 기준)
- 사용 통계 백업 (`/stats/{date}` 데이터)
- 운영자 UID 화이트리스트 유지 — 다음 회 운영 대비

---

## 5. 사고 대응 (Emergency)

### 운영자가 운행 중 자동 모드 의도치 않게 해제됨
- 자료 탭 → GPS 자동 다시 켜기 (LS:automode = 1 자동 복원)
- 시각 기준 재설정 (현재 시각 + 위치 기준)

### Firebase 연결 끊김
- 운영 모드 우상단 상태 칩 빨강 → 「재로그인」 클릭
- 30분 토큰 자동 갱신 — 문제 지속 시 페이지 새로고침

### 시연 모드 켜진 채 운영 시작 (사고)
- 화면 상단 🧪 시뮬 칩 노출 — 즉시 「해제」 클릭
- 자료 탭 → 시연·테스트 모드 → 「시연 모드 해제」 → 페이지 새로고침

### 라이브 신호 멈춤 (다른 사람 단말에서)
- 운영자 백업 단말 켜기 (같은 busId 충돌 시 경고)
- 행사 본부 → 직접 단말 확인

---

자세한 운영 매뉴얼은 별도 인쇄 자료 참고.
